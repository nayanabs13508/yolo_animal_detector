flask
ultralytics
opencv-python
Pillow
numpy
twilio
python-dotenv
# Chart.js is client-side via CDN; not needed in pip
# torch is required by ultralytics. Install the correct wheel for your system and CUDA following:
# https://pytorch.org/get-started/locally/
# Example CPU-only install:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers
flask>=2.0
ultralytics>=8.0.0
opencv-python
Pillow
numpy
requests
twilio
python-dotenv
transformers
torch
# Note: torch installation may require platform-specific wheel; if pip install fails,
# follow instructions at https://pytorch.org to install the correct torch+cuda/cpu wheel.flask>=2.2
ultralytics>=8.0
opencv-python>=4.5
python-dotenv>=0.21
requests>=2.28
twilio>=8.0
gunicorn>=20.1
# Note: uses yolov8s.pt (small model, better accuracy than nano)
